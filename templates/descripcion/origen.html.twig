{% extends 'base.html.twig' %}

{% block title %}Asistente de creación de conjunto de datos - Origen de los datos{% endblock title %}

{% block body %}
<div class="container" style="display:{{ permisoEdicion }}">
        <input type="hidden" id="existe" value="{{ existe }}"/>
		<div class="row">                  
        <div id="col02" class="col col-12">
             <div class="form formtheme01">
                <h1>Asistente de creación de conjunto de datos</h1>
                <div class="listadoPasos">
                    <ul>
                        <li class="paso01 complete">
                            <span>1</span>
                        </li>
                        <li class="paso02 complete">
                            <span>2</span>
                        </li>
                        <li class="paso04 active">
                            <span>3</span>
                        </li>
                        <li class="paso03 uncomplete">
                            <span>4</span>
                        </li>
                    </ul>
                </div>
                <div>
                  <div class="box">
                    {{ form_start(origen_form) }}
                    <div class="fieldset fieldset01">
                        <fieldset class="mediumLabels">
                            <legend>Paso 3</legend>
                            <h2>Estructura - Origen</h2>
                            <div>
                                {{ form_widget(origen_form) }}
                                <div >
                                    <div class="form-group" id="archivoCargado">
                                        <table>
                                            <tbody>
                                                <tr>
                                                    <td></td>
                                                </tr>
                                                 <tr><i>Archivo temporal</i><tr>
                                            </tbody>
                                        </table>
                                    </div>
                                    {% if not archivoActual is empty  %}
                                         <div class="form-group" id="archivoActual">
                                            <table>
                                                <tbody>
                                                    <tr>
                                                        <td> {{ archivoActual }} </td>
                                                        <td id="lineaArchivoActual">
                                                            <a id="borrarArchivoActual" name="borrarArchivoActual" class="remove"></a>
                                                        </td>
                                                    </tr>
                                                    <tr><i>Archivo subido al servidor</i><tr>
                                                </tbody>
                                            </table>
                                        </div>
                                    {% endif %}  
                                    <input id="comprobar_origen" name="comprobaro_rigen"  class="rounded" type="button" value="Comprobar origen" onclick="this.form.submit()">      
                                    {% if errorProceso %}
                                        <div class='ko'>
                                            <p> {{ errorProceso }} </p>
                                        </div>
                                    {% endif %}
                                    {% if campos %}
                                        <div class='ok'>
                                            <p> Origen de datos validado con los campos: </p>
                                            <ul>
                                            {% for campo in campos %}
                                                <li>
                                                  {{ campo }}
                                                </li>
                                            {% endfor %}
                                            </ul>
                                        </div>
                                    {% endif %}
                                </div>
                                <div class="fieldset actionButtons">
                                    <fieldset>
                                        {% if locationAnterior %}
                                            <input class="submit" value="Anterior" onclick="location.href='{{ locationAnterior }}'">
                                        {% endif %}
                                        {% if muestraSiguiente %}
                                            <input id="siguiente" class="submit principal" type="submit" value="Guardar y siguiente...">
                                        {% endif %}
                                    </fieldset>
                                </div>
                            </div>
                        </fieldset>
                    </div>
                   {{ form_end(origen_form) }}
                </div>
            </div>
        </div>
	</div>
</div>
<div class="modal fade" id="camposErrormodal" tabindex="-1" role="dialog" aria-hidden="true" style="display: none;">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Aviso</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <div class="modal-body">
                {% if camposDistintos %} 
                    <div id="muestaModal" />
                    <p>La estructura de campos actual, respecto a la que se aprobó ha cambiado.</p>
                    <p>Si continua perderá parte o la totalidad el alinemiento realizado.</p>
                    <p> Campos Alineados: </p>
                    <ul>
                        {% for campo in camposAlineados %}
                            <li>
                                {{ campo }}
                            </li>
                        {% endfor %}
                    </ul>                                                                            
                    <p> Campos Actuales: </p>  
                    <ul>
                        {% for campo in camposActuales %}
                            <li>
                                {{ campo }}
                            </li>
                        {% endfor %}
                    </ul>
                {% endif %}
                {% if existe %} 
                    <p>Ya existe un archivo con ese nombre y extensión.</p>
                    <p>Si continuas con el asistente el archivo será remplazado.</p>
                    <p>Si el archivo ya existente, pertenece a otra distribución de este origen de datos, los datos se perderán.</p>
                {% endif %}
                <input class="btn primary" type="button" value="Aceptar" data-dismiss="modal">
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
   {{ parent() }}
   <script type="text/javascript" src="/js/origen.js"></script>
{% endblock %}